# Minimal dev requirements — SQLite + eager Celery, fast install
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# SQLAlchemy with aiosqlite for local dev (no PostgreSQL required)
sqlalchemy==2.0.35
aiosqlite==0.20.0
alembic==1.13.3
pydantic-settings==2.5.2

python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

boto3==1.35.0

celery==5.4.0
# Redis not required in dev — tasks run eagerly (synchronously)

opencv-python-headless==4.10.0.84
Pillow==10.4.0
numpy==1.26.4

stripe==10.12.0
python-dotenv==1.0.1
httpx==0.27.2
