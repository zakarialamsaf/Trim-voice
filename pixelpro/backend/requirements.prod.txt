# Production requirements — CPU-only (for Railway / Render / any cloud without GPU)
# GPU features (Real-ESRGAN quality upscaling) are replaced by CPU fallback in prod
# AI Editing still works via Gradio spaces (no local GPU needed)

fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

sqlalchemy==2.0.35
asyncpg==0.29.0
aiosqlite==0.20.0
alembic==1.13.3
pydantic-settings==2.5.2

python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

boto3==1.35.0

celery==5.4.0
redis==5.1.0

# CPU-only torch (much smaller than CUDA build — ~250MB vs 2GB+)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.4.1+cpu
torchvision==0.19.1+cpu

opencv-python-headless==4.10.0.84
Pillow==10.4.0
numpy==1.26.4

basicsr==1.4.2
realesrgan==0.3.0

gfpgan==1.3.8

rembg==2.0.57
onnxruntime==1.19.2  # CPU version (not onnxruntime-gpu)

stripe==10.12.0

sentry-sdk[fastapi]==2.16.0

python-dotenv==1.0.1
httpx==0.27.2
gradio_client>=0.7.0
fal-client>=0.8.0
huggingface_hub>=0.24.0
